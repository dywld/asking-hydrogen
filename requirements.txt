# 서버 실행에 꼭 필요한 것 — 시작.bat 이 알아서 설치한다
numpy

# 아래는 있으면 좋고 없어도 서버는 뜬다
# sentence-transformers, torch   뜻 벡터 검색·재순위 (data/embeddings.npy 가 있을 때만 쓰임)
# pymupdf                        pipeline/ 의 PDF 처리, 근거 카드 "PDF n쪽" 버튼
# python-docx                    tools/make_report_kr.py 보고서 생성

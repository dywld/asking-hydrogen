# API 키 — 절대 올리지 않는다
key.txt

__pycache__/
*.pyc
server.pid
data/raw/
worker/.wrangler/
worker/node_modules/

# 배포용 사본 — export_dataset.py 로 언제든 다시 만든다
배포/

# 뜻 벡터 — embed_index.py 로 만든다(수십 MB, 재생성 가능)
data/embeddings*.npy
data/embeddings.json

# 이용자 질문 기록 — 개인정보라 올리지 않는다
data/chat_log.jsonl
_*.log

# 서버가 만드는 것들 — 답변 캐시·공유 대화·PDF 쪽 그림
data/answer_cache.json
data/shares/
data/pages/

# 이용자 피드백 — 개인정보라 올리지 않는다
data/feedback.jsonl

# 법령정보센터에서 받아 둔 서식·별표 파일 — forms.py 가 필요할 때 다시 받는다
data/forms/

# 윈도우 바로가기
*.lnk
